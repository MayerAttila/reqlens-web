const root = document.querySelector<HTMLDivElement>("#root");

if (root) {
  root.innerHTML = `
    <main style="font-family: sans-serif; padding: 48px;">
      <h1>Reqlens</h1>
      <p>Dashboard repo placeholder. Build after ingest data exists.</p>
    </main>
  `;
}
